---
layout: layouts/base.njk
permalink: "/series/"
title: Series
---
<section class="page-hero">
  <div class="page-hero-inner page-hero-inner--editorial">
    <div class="editorial-bar">
      <a href="/" class="editorial-back" aria-label="Voltar para a pagina inicial"><i class="fa-solid fa-chevron-left" aria-hidden="true"></i></a>
      <div class="editorial-brand">
        <p class="editorial-site">{{ site.name }}</p>
        <p class="editorial-hello">Series e o que acompanho</p>
      </div>
    </div>
  </div>
</section>

<section class="content-frame">
  <div class="content-surface">
    <header class="entry-header-card">
      {% set breadcrumb = [{label: "Séries"}] %}
      {% include "partials/breadcrumb.njk" %}
      <h1 class="entry-title">Series</h1>
      <p class="entry-summary">O que estou acompanhando, o que já terminei e o que ainda quero ver.</p>
    </header>

    {% set allSeries = collections.series | default([]) %}
    {% if allSeries | length > 0 %}
    <div class="media-grid">
      {% for item in allSeries %}
      <div class="media-card">
        <div class="media-card-poster">
          {{ item.data.poster or '📺' }}
          {% if item.data.status == 'done' %}<span class="media-badge media-badge--done">Assistida</span>
          {% elif item.data.status == 'watching' %}<span class="media-badge media-badge--watching">Assistindo</span>
          {% elif item.data.status == 'want' %}<span class="media-badge media-badge--want">Quero ver</span>
          {% endif %}
        </div>
        <div class="media-card-body">
          <p class="media-card-title">{{ item.data.title }}</p>
          <p class="media-card-meta">{{ item.data.platform }}{% if item.data.years %} · {{ item.data.years }}{% endif %}{% if item.data.seasons %} · {{ item.data.seasons }}T{% endif %}</p>
          {% if item.data.note %}<p class="media-card-note">{{ item.data.note }}</p>{% endif %}
        </div>
      </div>
      {% endfor %}
    </div>
    {% else %}
    <div class="empty-card">Nenhuma série ainda.</div>
    {% endif %}
  </div>
</section>
