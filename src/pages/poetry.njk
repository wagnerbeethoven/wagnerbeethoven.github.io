---
layout: layouts/base.njk
title: Poesias
pagination:
  data: collections.poetry
  size: 10
  alias: items
permalink: "/poetry/{% if pagination.pageNumber > 0 %}page-{{ pagination.pageNumber + 1 }}/{% endif %}"
---
<section class="page-hero">
  <div class="page-hero-inner page-hero-inner--editorial">
    <div class="editorial-bar">
      <a href="/" class="editorial-back" aria-label="Voltar para a pagina inicial"><i class="fa-solid fa-chevron-left" aria-hidden="true"></i></a>
      <div class="editorial-brand">
        <p class="editorial-site">{{ site.name }}</p>
      </div>
    </div>
  </div>
</section>

<section class="content-frame">
  <div class="content-surface">
    <header class="entry-header-card">
      {% set breadcrumb = [{label: "Poesias"}] %}
      {% include "partials/breadcrumb.njk" %}
      <h1 class="entry-title">Poesias</h1>
      {% include "partials/page-summary.njk" %}
    </header>

    {% set items = items | default([]) %}
    {% if items | length > 0 %}
    <div class="home-blog-list h-feed">
      {% for item in items %}
      <a href="{{ item.url }}" class="home-blog-row h-entry">
        <div class="home-blog-row-main">
          <span class="home-blog-row-title p-name">{{ item.data.author }}</span>
        </div>
        <span class="home-blog-row-date">{{ item.data.title or 'Sem título' }}</span>
      </a>
      {% endfor %}
    </div>
    {% else %}
    <div class="empty-card">Ainda não há poemas publicados.</div>
    {% endif %}

    {% set paginationLabel = "Paginação das poesias" %}
    {% set paginationPrevLabel = "Mais recentes" %}
    {% set paginationNextLabel = "Mais antigas" %}
    {% include "partials/collection-pagination.njk" %}
  </div>
</section>
