---
layout: layouts/base.njk
---
<section class="page-hero">
  <div class="page-hero-inner page-hero-inner--editorial">
    <div class="editorial-bar">
      <a href="/comics/" class="editorial-back" aria-label="Voltar para quadrinhos"><i class="fa-solid fa-chevron-left" aria-hidden="true"></i></a>
      <div class="editorial-brand">
        <p class="editorial-site">{{ site.name }}</p>
        <p class="editorial-hello">Quadrinhos</p>
      </div>
    </div>
  </div>
</section>

<section class="content-frame">
  <div class="content-surface media-page">
    <header class="entry-header-card">
      {% set breadcrumb = [{label: "Quadrinhos", url: "/comics/"}, {label: title}] %}
      {% include "partials/breadcrumb.njk" %}
      <h1 class="entry-title">{{ title }}</h1>
      {% if writer %}<p class="media-page-subtitle">{{ writer }}</p>{% endif %}
    </header>

    <div class="media-detail">
      <div class="media-poster media-poster--portrait">
        {% if image %}
          <img src="{{ image }}" alt="Capa de {{ title }}" class="media-poster-img" loading="lazy">
        {% else %}
          <div class="media-poster-placeholder" aria-hidden="true">💬</div>
        {% endif %}
      </div>

      <div class="media-detail-info">
        <dl class="media-meta">
          {% if writer %}<div class="media-meta-wide"><dt>Roteiro</dt><dd>{{ writer }}</dd></div>{% endif %}
          {% if artist %}<div class="media-meta-wide"><dt>Arte</dt><dd>{{ artist }}</dd></div>{% endif %}
          {% if publisher %}<div><dt>Editora</dt><dd>{{ publisher }}</dd></div>{% endif %}
          {% if year %}<div><dt>Ano</dt><dd>{{ year }}</dd></div>{% endif %}
          {% if volumes %}<div><dt>Volumes</dt><dd>{{ volumes }}</dd></div>{% endif %}
        </dl>

        {% if tags %}
          <ul class="inline-pills" aria-label="Tags">
            {% for tag in tags %}
              <li><a class="pill p-category" href="/tags/#tag-{{ tag | slug }}">{{ tag }}</a></li>
            {% endfor %}
          </ul>
        {% endif %}
      </div>
    </div>

    {% set synopsisText = synopsis or (content | striptags | trim) %}
    {% if synopsisText %}
      <h2 class="media-section-title">Sinopse</h2>
      <div class="media-text-block">
        <p class="media-note">{{ synopsisText }}</p>
      </div>
    {% endif %}

    {% if note %}<p class="media-note">{{ note }}</p>{% endif %}
    {% if content and synopsis %}<div class="prose">{{ content | safe }}</div>{% endif %}

    {% include "partials/share-links.njk" %}
  </div>
</section>
