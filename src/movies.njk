---
layout: layouts/base.njk
permalink: "/movies/"
title: Filmes
showUpdated: true
---
<section class="page-hero">
  <div class="page-hero-inner page-hero-inner--editorial">
    <div class="editorial-bar">
      <a href="/" class="editorial-back" aria-label="Voltar para a página inicial"><i class="fa-solid fa-chevron-left" aria-hidden="true"></i></a>
      <div class="editorial-brand">
        <p class="editorial-site">{{ site.name }}</p>
        <p class="editorial-hello">Cinema e o que fica</p>
      </div>
    </div>
  </div>
</section>

<section class="content-frame">
  <div class="content-surface">
    <header class="entry-header-card">
      {% set breadcrumb = [{label: "Filmes"}] %}
      {% include "partials/breadcrumb.njk" %}
      <h1 class="entry-title">Filmes</h1>
      <p class="entry-summary">Um registro do que assisti, do que ficou e do que ainda quero ver.</p>
      {%- include "partials/last-updated.njk" %}
    </header>

    {% set allMovies = collections.movies | default([]) %}
    {% if allMovies | length > 0 %}
    <div class="media-grid">
      {% for item in allMovies %}
      <a href="{{ item.url }}" class="media-card" aria-label="{{ item.data.title }}">
        <div class="media-card-poster">
          {% if item.data.image %}
            <img src="{{ item.data.image }}" alt="" loading="lazy">
          {% elif item.data.poster %}
            <img src="{{ item.data.poster }}" alt="" loading="lazy">
          {% else %}
            🎬
          {% endif %}
          {% if item.data.status == 'done' %}<span class="media-badge media-badge--done">Assistido</span>
          {% elif item.data.status == 'watching' %}<span class="media-badge media-badge--watching">Assistindo</span>
          {% endif %}
          <span class="media-card-cover-label">{{ item.data.title }}</span>
        </div>
      </a>
      {% endfor %}
    </div>
    {% else %}
    <div class="empty-card">Nenhum filme ainda.</div>
    {% endif %}
  </div>
</section>
