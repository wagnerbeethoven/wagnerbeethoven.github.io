---
layout: layouts/base.njk
permalink: "/games/"
title: Jogos
showUpdated: true
---
<section class="page-hero">
  <div class="page-hero-inner page-hero-inner--editorial">
    <div class="editorial-bar">
      <a href="/" class="editorial-back" aria-label="Voltar para a página inicial"><i class="fa-solid fa-chevron-left" aria-hidden="true"></i></a>
      <div class="editorial-brand">
        <p class="editorial-site">{{ site.name }}</p>
        <p class="editorial-hello">Jogos e o que está rolando</p>
      </div>
    </div>
  </div>
</section>

<section class="content-frame">
  <div class="content-surface">
    <header class="entry-header-card">
      {% set breadcrumb = [{label: "Jogos"}] %}
      {% include "partials/breadcrumb.njk" %}
      <h1 class="entry-title">Jogos</h1>
      <p class="entry-summary">Um registro do que joguei, do que estou jogando e da lista de espera.</p>
      {%- include "partials/last-updated.njk" %}
    </header>

    {% set allGames = collections.games | default([]) %}
    {% if allGames | length > 0 %}
    <div class="media-grid">
      {% for item in allGames %}
      <a href="{{ item.url }}" class="media-card" aria-label="{{ item.data.title }}">
        <div class="media-card-poster">
          {% if item.data.image %}
            <img src="{{ item.data.image }}" alt="" loading="lazy">
          {% elif item.data.poster %}
            <img src="{{ item.data.poster }}" alt="" loading="lazy">
          {% else %}
            🎮
          {% endif %}
          <span class="media-card-cover-label">{{ item.data.title }}</span>
        </div>
      </a>
      {% endfor %}
    </div>
    {% else %}
    <div class="empty-card">Nenhum jogo ainda.</div>
    {% endif %}
  </div>
</section>
